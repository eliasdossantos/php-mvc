<!-- app/Views/{{ viewPath }}/show.php -->
<div class="page-header">
    <div>
        <a href="<?= url('dashboard/{{ routePrefix }}') ?>" class="btn btn-outline btn-sm">
            ← Voltar
        </a>
        <h1 style="margin-top:12px"><?= e($title ?? 'Detalhes') ?></h1>
    </div>
    <a href="<?= url("dashboard/{{ routePrefix }}/{$item->id}/edit") ?>"
       class="btn btn-secondary">
        Editar
    </a>
</div>

<?php \Core\View::render('components.alerts'); ?>

<div class="card">
    <div class="card-body">
        <table class="doc-table">
            <tbody>
                <tr>
                    <td><strong>ID</strong></td>
                    <td><?= e($item->id ?? '—') ?></td>
                </tr>
                <tr>
                    <td><strong>Nome</strong></td>
                    <td><?= e($item->name ?? '—') ?></td>
                </tr>
                <tr>
                    <td><strong>Criado em</strong></td>
                    <td><?= dateBR($item->created_at ?? null) ?></td>
                </tr>
                <tr>
                    <td><strong>Atualizado em</strong></td>
                    <td><?= dateBR($item->updated_at ?? null) ?></td>
                </tr>
                <!-- Adicione mais campos conforme o model -->
            </tbody>
        </table>
    </div>
</div>

<div class="mt-2">
    <form method="POST"
          action="<?= url("dashboard/{{ routePrefix }}/{$item->id}") ?>"
          style="display:inline">
        <?= csrf_field() ?>
        <?= method_field('DELETE') ?>
        <button type="submit"
                class="btn btn-danger"
                data-confirm="Excluir este {{ ModelName }} permanentemente?">
            Excluir
        </button>
    </form>
</div>
